import React from 'react';
import './Footer.css';

const Footer = () => {
    return(
        <footer>
            <p>some info</p>
            <p>some info</p>
            <p>some info</p>
        </footer>
    );
};

export default Footer;